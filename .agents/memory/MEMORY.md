- [Balance & Activity Tracker](tracker-app.md) — xlsx-driven append-only import/records app; live ageing, jsonb summary, cross-upload hash dedup, parse rules.
- [Tracker ageing source](tracker-ageing.md) — ageing date is per-activity: C (cutting) ages from Assign Date, all others from col S Last Production Entry Date; future→0, blank→null; no assign-date fallback for non-C.
- [Numeric input pattern](numeric-input-pattern.md) — controlled type=number bound to a parsed number reverses digits (90→09); use the NumberInput raw-draft component for any numeric field.
- [Classification backfill](classification-backfill.md) — new derived pool columns stay NULL on legacy rows (onConflictDoNothing never updates); tolerate NULL in view filters AND run an explicit boot backfill from stored raw columns.
- [TLT/NTLT/All category mode](tracker-category-mode.md) — Order Type mode (ALL/TLT/NTLT, default TLT) switches filter dimension (Project↔Section) + grouping; All includes both via per-row, prefixed keys (never coerce All→TLT on single-dimension pages).
- [Thickness lookup + markId hazard](tracker-thickness.md) — thickness is live-resolved (never hashed); DELETE endpoints keyed on markId/groupKey must use query params, not path segments (backslashes/spaces break path routing).
- [TLT/NTLT category sequences](tracker-categories.md) — marks follow per-category process sequences (TLT 12-step default, NTLT RSJ/Earthing/General shorter); resolve sequenceFor(record) before any "before Y" test; classification fields not in hash.
- [Tracker milestones (Ready/Dispatched)](tracker-milestones.md) — capture-once; dispatch-on-disappear must NOT rely only on replayable import_rows (deletion cascades them) — persist advance-forward-only last_seen pointer.
- [Contractor sub-categories](tracker-contractor-categories.md) — additive display-only classification overlay (CNC/Sub/Out-vendor + FAB/GALVA; "CNC" was renamed from "In-house"); joined live on a normalized name key; never mutates contractor strings.
- [Order Status & Dispatch](tracker-order-status.md) — second "Order Review" file: two-row header, sets=integer, idempotent UPSERT under advisory lock, dispatch seed-once+ledger; "0 everywhere" is usually WIP coverage not a bug; grouped subtotals must aggregate EVERY displayed numeric column.
- [Sticky table header/footer](tracker-sticky-tables.md) — ui/table.tsx Table has its own internal overflow-auto wrapper; cap height via containerClassName (not an outer div) so sticky thead/tfoot/totals pin.
- [Client/server aggregation parity](tracker-server-aggregation.md) — shared @workspace/domain filter+aggregators run on both sides for byte-identical numbers; date windows MUST use calendar day-keys (YYYYMMDD), never epoch ms, or server timezone shifts rows at window edges.
- [Master home + /production nesting](tracker-master-home.md) — VTPL hub at `/` (dept tiles); whole tracker nested under `/production` (wouter nest); use `~/` to escape to root; legacy root paths redirect.
- [In-Hand fab load route guard](tracker-fab-load-route-guard.md) — upcoming (In Hand) fab load for W/B counts a mark only if the op is in its Col Q route; blank-Col-Q falls back to include; never touch Operational/punch/drill/quality.
- [OpenAPI date fields → zod coerce.date()](openapi-date-format-zod.md) — `format: date` makes Orval emit `zod.coerce.date()` (parses to Date), silently nulling YYYY-MM-DD string fields the domain expects as strings; use `pattern` instead.
- [Galvanizing grouping (G,GB,Y) vs Yard column](tracker-galvanizing-grouping.md) — GALVANIZING bundle spans G,GB,Y (filter + Plant Op tab); tables with a separate Yard column use GALVANIZING−YARD; drill via hidden GALVANIZING_CORE bundle; Project-Wise Ready-for-Dispatch/FG col is currently unwired ("-") by user request.
- [Global WIP valid-from cutoff](tracker-wip-cutoff.md) — nullable settings.validFromDate scopes whole app to imports on/after it (reportDate else UTC-day of createdAt); null = byte-identical; cutoffSql(null)=undefined no-op; milestone persist only when cutoff null.
- [MFC batch grouping + 21-col hash](tracker-mfc-grouping.md) — WIP file gained col T Work Order No. + col U MFC batch; hash now 21 cols (raw pre-Z batch); MFC is an additive TLT grouping level Project→MFC→Structure→Mark.
- [Finished Good figures + order balances](tracker-computed-fg.md) — one FG figure: Overview=file Galv−Dispatch (useFgRows); Accumulated WIP + FG WIP Computed fully removed; order balances base on Col J.
- [Contractor Performance movement ledger](tracker-contractor-movement.md) — daily activity-to-activity moves credited to the FROM contractor; Fab/Galv stage derived from fromActivity (TS/Y), per-contractor Excel sheets, reused on Contractor Wise page.
- [Diagnosing "X shows nothing" report complaints](tracker-empty-stage-diagnosis.md) — before assuming a bug, replicate the frontend classifier in Node against live API/DB data; an empty bucket can be a real data fact (no qualifying transitions occurred), not a code defect.
- [markId+jobCardNo identity bridge](tracker-identity-bridge.md) — the shop reissues a new job card every time a TLT mark advances a fabrication stage, breaking the markId+jobCardNo continuity key used by diff.ts/contractorMovement.ts; bridged via unambiguous 1-old-card→1-new-card swap detection in identityBridge.ts.
- [App-wide date display format](tracker-date-format.md) — every user-facing date must render dd-mm-yyyy via shared formatDate/formatDateTime helpers; export column labels are the one intentional exception (raw ISO for spreadsheet consumers).
- [Live-balance completion vs moves-log stage](tracker-fab-galv-completion.md) — "Fabrication/Galvanizing complete" (live zero balance across an activity set) is a distinct, additive concept from the existing moves-log "left TS/left Y" stage counts — don't conflate the two when a bundle's set boundary differs per consumer.
- [Year-aware relative date presets](tracker-relative-date-presets.md) — never hardcode "Q1..Q4" tied to the current calendar year; compute month/quarter filter presets relative to today, excluding the in-progress period, and always show the year in the label.
- [Contractor scope exit on blank contractor](tracker-contractor-scope-exit.md) — a mark whose contractor field is currently blank means that contractor's work is done (mark now with VTPL); its full move history is excluded from Contractor Performance, not just its current state.
- [Contractor Performance tabs + filter scope](tracker-contractor-performance.md) — ledger-derived sections can only honor filters mapping onto move-entry fields (Job/Contractor/Activity/Date/Search); live-balance section uses useFilteredRecords for full parity; card redesigned into Summary/Fab-Galv/Detail tabs.
- [Orval response-schema naming collision](orval-response-schema-naming.md) — never name an OpenAPI response schema exactly `Capitalize(operationId) + "Response"` — orval auto-generates a same-named zod const that collides with the schema's own type export.
- [Order Review "latest import" scoping](tracker-order-review-latest-only.md) — order_review_rows is UPSERTed across ALL imports ever loaded; features anchored to "the latest import" must filter WHERE importId = newest id, not scan the whole table.
- [Inventory Excel export](tracker-inventory-export.md) — per-bucket sheets reuse the same summary helpers as the on-screen footer; export must apply the active Job filter like the buckets do.
- [Current Jobs filter rollout gap](tracker-current-jobs-rollout.md) — a global filter-mode sentinel (e.g. Current Jobs) must be threaded through EVERY page reading `filters.job` directly, not just the shared hook — audit each page for direct `filters.job` reads.
- [Bucket B/C/D mixed-side double counting](tracker-inventory-mixed-side.md) — Inventory buckets B/C/D duplicate a structure onto BOTH in-house and out-vendor totals when it's touched by contractors of both sides; combined "grand total" = sum of both side totals, not raw distinct-row sum.
- [Excel list-upload column detection](tracker-xlsx-header-detection.md) — a plain-list upload (e.g. project codes) must scan the first ~10 rows for a header token instead of assuming column 0, and must skip trailing "Total"/"Grand Total" footer rows.
- [WIP file format v2 (Jul 2026)](tracker-wip-format-v2.md) — newer WIP exports have 24 cols; "WO Batch No." renamed to "Batch No." (col X); parser falls back to either name so both old and new files work.
- [Initial Cutting exclusion](tracker-initial-cutting.md) — is_initial_cutting boolean on record_pool; marks with Type=JCNS+Status=Initial excluded from ALL Cutting balance figures; backfill proxy: activity=C AND assign_date IS NULL AND contractor IS NULL.
- [Cutting date-filter + is_initial_cutting fix](tracker-cutting-initial-flags.md) — null-assignDate records wrongly excluded by date window in aggregate.ts + job-dashboard; merge pre-check blocked is_initial_cutting update on re-upload; proxy SQL over-counts by ~2,764; exact fix = re-upload WIP file.
- [Inventory MFC backfill colour](tracker-inventory-mfc-color.md) — per-MFC-batch colour (green/white/yellow) stored in `inventory_mfc_color` table; shown as dot+swatch-picker in MfcTopRow; `_bgColor` ARGB field on export rows picked up by writeSheet to apply cell fill.
